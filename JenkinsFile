pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage...'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage (demo)...'
            }
        }
    }
}